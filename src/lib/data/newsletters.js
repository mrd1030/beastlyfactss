// All posts that used to live here have been superseded by their MDX
// equivalents under content/guides/ (old URLs already 301 in _redirects) —
// kept as an empty export so Blog.jsx's `localPosts` spread is a no-op
// rather than requiring a call-site change.
export const blogPosts = [];
