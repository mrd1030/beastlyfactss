# Hedgehog router hub + encyclopedia draft

Branch: `grok/hedgehog-router-hub` (from main `5cbc593`)
Date: 2026-09-10
Status: draft only. The live objects in `src/lib/data/guides/smallMammals.js` and
`src/lib/data/encyclopedia/smallMammals.js` were not overwritten. Splice the
objects in `hedgehog-hub.router.js` and `hedgehog-encyclopedia.js` after you
veto any sentence.

This is not a full reader-extract set test. It is the router conversion the
READMEFIRST hub job describes, using numbers already on the hedgehog deep dives.

## What this draft does

Hedgehog is still the old `sections` care sheet. Hamster in the same file is
already `layout: "router"`. The live hedgehog hub is also behind its own articles:
WHS listed as 1 in 3 (health guide: closer to 3%), act-at-65F (setup/health: 72F),
wheel 10-12 in (setup: 10.5-12), and no legal row.

Hub rows follow the sourced deep dive. Cost-guide legal FAQ still disagrees with
the legal guide (Maine). That stays a next job.

Do not merge this branch as a site change until the objects are spliced into
`smallMammals.js` and the checkers have run.
